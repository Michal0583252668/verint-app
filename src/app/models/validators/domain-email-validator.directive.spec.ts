import { DomainEmailValidatorDirective } from './domain-email-validator.directive';

describe('DomainEmailValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DomainEmailValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
